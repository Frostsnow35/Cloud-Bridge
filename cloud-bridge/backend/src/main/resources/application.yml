spring:
  application:
    name: cloud-bridge-backend
  datasource:
    # Use H2 for MVP if Docker is not available
    url: jdbc:h2:file:./data/cloud_bridge;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB
  data:
    elasticsearch:
      client:
        reactive:
          endpoints: ${SPRING_ELASTICSEARCH_URIS:localhost:9200}
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: 6379
      repositories:
        enabled: false
    neo4j:
      repositories:
        enabled: false
  neo4j:
    uri: ${SPRING_NEO4J_URI:bolt://localhost:7687}
    authentication:
      username: ${SPRING_NEO4J_AUTHENTICATION_USERNAME:neo4j}
      password: ${SPRING_NEO4J_AUTHENTICATION_PASSWORD:password} 

es:
  url: ${ES_URL:http://localhost:9200}

ai:
  api:
    url: ${AI_API_URL:https://api.deepseek.com/chat/completions}
    key: sk-da88a9736b9542489918d4b7afa88a99
    model: ${AI_API_MODEL:deepseek-chat}
    embedding-url: ${AI_EMBEDDING_URL:https://integrate.api.nvidia.com/v1/embeddings}
    embedding-key: ${AI_EMBEDDING_KEY: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}
    embedding-model: ${AI_EMBEDDING_MODEL:nvidia/nv-embedqa-e5-v5}
  agent:
    # Agent 编排配置
    max-iterations: 8          # 最大工具调用轮数
    timeout-seconds: 120       # 超时时间
    model: ${AI_AGENT_MODEL:deepseek-chat}  # Agent 编排专用模型，不配置则回退使用 ai.api.model
    temperature: 0.1           # 工具调用时使用低温度确保可靠性
    max-tokens: 2048           # 单次响应最大 token 数

fisco:
  network:
    peers:
      - "127.0.0.1:20200"
  cryptoMaterial:
    certPath: "conf"
  account:
    keyPath: "conf"
    accountAddress: ""
  contract:
    evidenceAddress: ""

file:
  upload-dir: uploads

server:
  port: 8080

logging:
  file:
    name: backend.log
  level:
    root: INFO
    com.cloudbridge: DEBUG
    org.neo4j: OFF
    org.springframework.data.neo4j: OFF
