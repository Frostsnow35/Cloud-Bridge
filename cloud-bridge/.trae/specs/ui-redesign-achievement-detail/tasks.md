# Tasks for UI Redesign

- [ ] **Task 1: Header Optimization**:
  - [ ] Update `detail-header` layout using Flexbox/Grid for better alignment.
  - [ ] Style the title with a larger font weight and size.
  - [ ] Refine tag styles (e.g., `effect="light"` or custom CSS).
  - [ ] Style the price to be prominent but elegant.
  - [ ] Update action buttons with gradients or shadows.

- [ ] **Task 2: Content Layout & Typography**:
  - [ ] Increase padding in `content-left` and `sidebar`.
  - [ ] Update `.section-title` with a modern marker (e.g., a colored vertical bar or icon).
  - [ ] Set `line-height` to 1.6 or 1.8 for `.rich-text`.
  - [ ] Add `margin-bottom` to separate sections clearly.

- [ ] **Task 3: Card Styling (Services, Team, Recs)**:
  - [ ] Apply a consistent `border-radius` (e.g., 12px or 16px) to all cards.
  - [ ] Add a subtle `box-shadow` to cards.
  - [ ] Implement a hover effect (transform: translateY(-5px)) for interactive cards.
  - [ ] Style the `service-card` with a glass-morphism effect if possible (background-color with opacity + backdrop-filter).

- [ ] **Task 4: Patent Carousel Polish**:
  - [ ] Ensure images in the carousel cover the area without distortion (`object-fit: cover`).
  - [ ] Style the carousel indicators if possible.

- [ ] **Task 5: Final Polish**:
  - [ ] Check responsive behavior (mobile/tablet).
  - [ ] Verify color contrast accessibility.
