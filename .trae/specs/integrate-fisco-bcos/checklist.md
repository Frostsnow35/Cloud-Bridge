# Checklist

- [x] `pom.xml` contains `fisco-bcos-java-sdk`.
- [x] `Evidence.sol` supports `evidenceType`, `signerId`.
- [x] `FiscoBcosService.java` exists and uses `BcosSDK`.
- [x] `application.yml` has `fisco` configuration placeholders (IP, Port, Certs).
- [x] Backend compiles without errors.
