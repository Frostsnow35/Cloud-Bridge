# Checklist

- [ ] Backend logs show "Seeded X public platforms from CSV".
- [ ] API `/api/libraries/public_platforms` returns a JSON array with > 2 items.
- [ ] Frontend "Public Platforms" page displays the data list.
